<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShortStory - Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #f8fafc; font-family: 'Inter', sans-serif; }
        .card { transition: transform 0.2s; border: 2px solid #e2e8f0; }
        .card:hover { transform: translateY(-5px); }
    </style>
</head>
<body class="p-4 md:p-8">
    <nav class="max-w-6xl mx-auto flex justify-between items-center mb-10">
        <h1 class="text-3xl font-bold text-slate-800 tracking-tight">STORY<span class="text-blue-600">APP</span></h1>
        <div class="flex gap-4">
            <a href="/upload" class="bg-blue-600 text-white px-6 py-2 rounded-lg font-semibold hover:bg-blue-700">Tulis Cerita</a>
        </div>
    </nav>

    <div class="max-w-6xl mx-auto">
        <!-- Search Bar -->
        <form action="/" method="GET" class="mb-8">
            <input type="text" name="search" placeholder="Cari judul atau genre..." value="<%= search %>" class="w-full p-4 rounded-xl border-2 border-slate-200 focus:border-blue-500 outline-none">
        </form>

        <!-- Grid Stories -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% stories.forEach(story => { %>
                <div class="card bg-white rounded-2xl overflow-hidden shadow-sm flex flex-col">
                    <div class="p-4 flex items-center justify-between border-b border-slate-50">
                        <div class="flex items-center gap-3">
                            <img src="<%= story.authorImg %>" class="w-10 h-10 rounded-full object-cover border border-slate-200">
                            <div>
                                <p class="text-sm font-bold text-slate-700"><%= story.authorName %></p>
                                <p class="text-xs text-slate-400">Penulis</p>
                            </div>
                        </div>
                        <span class="bg-slate-100 text-slate-600 text-[10px] px-3 py-1 rounded-full font-bold uppercase tracking-wider"><%= story.genre %></span>
                    </div>
                    
                    <img src="<%= story.coverImg %>" class="w-full h-48 object-cover">
                    
                    <div class="p-5 flex-grow">
                        <h2 class="text-xl font-bold text-slate-800 mb-2"><%= story.title %></h2>
                        <p class="text-slate-500 text-sm line-clamp-3 mb-4"><%= story.storyText %></p>
                    </div>

                    <div class="p-5 pt-0">
                        <a href="/story/<%= story.id %>" class="block text-center w-full py-3 bg-slate-800 text-white rounded-xl font-bold hover:bg-slate-900 transition">Baca Selengkapnya</a>
                    </div>
                </div>
            <% }) %>
        </div>

        <% if(stories.length === 0) { %>
            <div class="text-center py-20">
                <p class="text-slate-400 text-lg">Belum ada cerita. Jadilah yang pertama menulis!</p>
            </div>
        <% } %>
    </div>
</body>
</html>
