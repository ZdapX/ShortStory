<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShortStory - Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#f3f4f6] font-sans text-slate-900">
    <header class="bg-white border-b border-slate-200 sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
            <h1 class="text-2xl font-black text-blue-600 tracking-tighter">STORY.ID</h1>
            <a href="/upload" class="bg-blue-600 text-white px-5 py-2 rounded-xl font-bold text-sm hover:bg-blue-700 transition">Tulis Cerita</a>
        </div>
    </header>

    <main class="max-w-6xl mx-auto px-4 py-8">
        <form action="/" method="GET" class="mb-10">
            <input type="text" name="search" placeholder="Cari judul atau genre..." value="<%= search %>" 
            class="w-full bg-white p-4 rounded-2xl border border-slate-200 shadow-sm focus:ring-2 focus:ring-blue-500 outline-none transition">
        </form>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% if (stories && stories.length > 0) { %>
                <% stories.forEach(story => { %>
                    <div class="bg-white rounded-3xl overflow-hidden border border-slate-200 shadow-sm hover:shadow-md transition flex flex-col">
                        <div class="p-4 flex items-center gap-3">
                            <img src="<%= story.authorImg %>" class="w-10 h-10 rounded-full object-cover bg-slate-100 border border-slate-100">
                            <div class="flex-grow">
                                <p class="text-sm font-bold text-slate-800"><%= story.authorName %></p>
                                <p class="text-[10px] text-slate-400 uppercase font-bold tracking-widest"><%= story.genre %></p>
                            </div>
                        </div>
                        
                        <a href="/story/<%= story.id %>">
                            <img src="<%= story.coverImg %>" class="w-full h-52 object-cover bg-slate-200">
                        </a>
                        
                        <div class="p-5 flex-grow">
                            <h2 class="text-xl font-bold mb-2 line-clamp-1"><%= story.title %></h2>
                            <p class="text-slate-500 text-sm line-clamp-3"><%= story.storyText %></p>
                        </div>
                        
                        <div class="p-5 pt-0">
                            <a href="/story/<%= story.id %>" class="block text-center py-3 bg-slate-900 text-white rounded-xl font-bold hover:bg-black transition">Baca Sekarang</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full text-center py-20 bg-white rounded-3xl border border-dashed border-slate-300">
                    <p class="text-slate-400 font-medium">Belum ada cerita. Ayo mulai menulis!</p>
                </div>
            <% } %>
        </div>
    </main>
</body>
</html>
