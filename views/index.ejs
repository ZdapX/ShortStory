<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShortStory - Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 font-sans text-slate-900">
    <header class="bg-white border-b border-slate-200 sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
            <h1 class="text-2xl font-black text-blue-600 tracking-tighter">STORY.COM</h1>
            <a href="/upload" class="bg-blue-600 text-white px-5 py-2 rounded-full font-bold text-sm hover:bg-blue-700">Tulis Cerita</a>
        </div>
    </header>

    <main class="max-w-6xl mx-auto px-4 py-8">
        <!-- Search -->
        <form action="/" method="GET" class="mb-10">
            <input type="text" name="search" placeholder="Cari judul atau genre cerita..." value="<%= search %>" 
            class="w-full bg-white p-4 rounded-2xl border border-slate-200 shadow-sm focus:ring-2 focus:ring-blue-500 outline-none transition">
        </form>

        <!-- Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% stories.forEach(story => { %>
                <div class="bg-white rounded-3xl overflow-hidden border border-slate-200 shadow-sm hover:shadow-md transition">
                    <div class="p-4 flex items-center gap-3">
                        <img src="<%= story.authorImg %>" class="w-10 h-10 rounded-full object-cover bg-slate-100">
                        <div class="flex-grow">
                            <p class="text-sm font-bold text-slate-800 leading-none"><%= story.authorName %></p>
                            <p class="text-xs text-slate-400 mt-1">Penulis</p>
                        </div>
                        <span class="bg-blue-50 text-blue-600 text-[10px] px-2.5 py-1 rounded-lg font-bold uppercase"><%= story.genre %></span>
                    </div>
                    
                    <a href="/story/<%= story.id %>">
                        <img src="<%= story.coverImg %>" class="w-full h-52 object-cover bg-slate-200">
                    </a>
                    
                    <div class="p-5">
                        <h2 class="text-xl font-bold mb-2 line-clamp-1"><%= story.title %></h2>
                        <p class="text-slate-500 text-sm line-clamp-3 mb-5"><%= story.storyText %></p>
                        <a href="/story/<%= story.id %>" class="block text-center py-3 bg-slate-100 text-slate-800 rounded-xl font-bold hover:bg-slate-200 transition">Baca Cerita</a>
                    </div>
                </div>
            <% }) %>
        </div>

        <% if(stories.length === 0) { %>
            <div class="text-center py-20">
                <p class="text-slate-400">Belum ada cerita yang ditemukan.</p>
            </div>
        <% } %>
    </main>
</body>
</html>
