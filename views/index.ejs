<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>STORY.ID - Baca & Tulis Cerita Pendek</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .no-scrollbar::-webkit-scrollbar { display: none; }
        .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
    </style>
</head>
<body class="bg-slate-50 font-sans text-slate-900">
    <!-- Navbar -->
    <nav class="bg-white border-b border-slate-200 sticky top-0 z-50">
        <div class="max-w-6xl mx-auto px-4 h-16 flex items-center justify-between">
            <a href="/" class="text-2xl font-black text-blue-600 tracking-tighter">STORY.ID</a>
            <div class="flex items-center gap-3">
                <% if (user) { %>
                    <a href="/upload" class="bg-blue-600 text-white px-4 py-2 rounded-xl font-bold text-xs md:text-sm hover:bg-blue-700 transition">Tulis Cerita</a>
                    <a href="/profile/<%= user.id %>" class="flex items-center gap-2">
                        <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center text-blue-600 font-bold text-xs border border-blue-200"><%= user.name[0] %></div>
                    </a>
                <% } else { %>
                    <a href="/login" class="text-slate-600 font-bold text-xs md:text-sm">Masuk</a>
                    <a href="/register" class="bg-blue-600 text-white px-4 py-2 rounded-xl font-bold text-xs md:text-sm hover:bg-blue-700 transition">Daftar</a>
                <% } %>
            </div>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto px-4 py-6">
        <!-- Search Bar -->
        <form action="/" method="GET" class="mb-6">
            <input type="text" name="search" placeholder="Cari judul cerita atau penulis..." value="<%= search %>" 
            class="w-full bg-white p-4 rounded-2xl border border-slate-200 shadow-sm focus:ring-2 focus:ring-blue-500 outline-none transition">
        </form>

        <!-- Genre Filter Scrollable -->
        <div class="flex gap-2 overflow-x-auto pb-6 no-scrollbar">
            <% ['Romance', 'Horror', 'Action', 'Fantasy', 'Slice of Life'].forEach(genre => { %>
                <a href="/?genre=<%= genre %>" 
                   class="px-5 py-2 rounded-full text-xs font-bold whitespace-nowrap transition <%= currentGenre === genre ? 'bg-blue-600 text-white shadow-lg shadow-blue-100' : 'bg-white border border-slate-200 text-slate-500 hover:bg-slate-100' %>">
                   <%= genre %>
                </a>
            <% }) %>
            <% if(currentGenre) { %> 
                <a href="/" class="px-5 py-2 text-red-500 text-xs font-bold whitespace-nowrap">Hapus Filter ×</a> 
            <% } %>
        </div>

        <!-- Story Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if (stories && stories.length > 0) { %>
                <% stories.forEach(story => { %>
                    <div class="bg-white rounded-[2rem] overflow-hidden border border-slate-200 shadow-sm hover:shadow-md transition flex flex-col">
                        <div class="p-4 flex items-center gap-3">
                            <% if (story.authorId) { %>
                                <a href="/profile/<%= story.authorId %>" class="flex items-center gap-3 flex-grow">
                            <% } else { %>
                                <div class="flex items-center gap-3 flex-grow">
                            <% } %>
                                <img src="<%= story.authorImg %>" class="w-9 h-9 rounded-full object-cover bg-slate-100 border border-slate-100" onerror="this.src='https://ui-avatars.com/api/?name=User'">
                                <div class="flex-grow">
                                    <p class="text-sm font-bold text-slate-800 leading-none"><%= story.authorName %></p>
                                    <p class="text-[10px] text-slate-400 font-bold uppercase tracking-widest mt-1"><%= story.genre %></p>
                                </div>
                            <% if (story.authorId) { %> </a> <% } else { %> </div> <% } %>
                        </div>
                        
                        <a href="/story/<%= story.id %>">
                            <img src="<%= story.coverImg %>" class="w-full h-52 object-cover bg-slate-100" onerror="this.src='https://via.placeholder.com/400x250?text=No+Image'">
                        </a>
                        
                        <div class="p-5 flex-grow">
                            <h2 class="text-lg font-black mb-2 line-clamp-1 leading-tight"><%= story.title %></h2>
                            <p class="text-slate-500 text-xs line-clamp-3 mb-4 leading-relaxed"><%= story.storyText %></p>
                            <div class="flex items-center gap-4 text-[11px] font-bold text-slate-400 border-t border-slate-50 pt-4">
                                <span class="flex items-center gap-1">❤️ <%= story.likes ? story.likes.length : 0 %></span>
                                <span class="flex items-center gap-1">💬 <%= story.comments ? story.comments.length : 0 %></span>
                            </div>
                        </div>
                        
                        <div class="p-5 pt-0">
                            <a href="/story/<%= story.id %>" class="block text-center py-3 bg-slate-900 text-white rounded-2xl font-bold text-xs hover:bg-black transition">Baca Sekarang</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full text-center py-24 bg-white rounded-[2.5rem] border border-dashed border-slate-300">
                    <p class="text-slate-400 font-medium italic">Belum ada cerita. Jadilah yang pertama menulis!</p>
                </div>
            <% } %>
        </div>
    </main>
</body>
</html>
