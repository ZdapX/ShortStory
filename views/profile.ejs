<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8"><script src="https://cdn.tailwindcss.com"></script></head>
<body class="bg-slate-50">
    <div class="max-w-4xl mx-auto py-12 px-6">
        <div class="bg-white p-10 rounded-[3rem] border border-slate-200 text-center mb-12 shadow-sm">
            <img src="<%= profile.profilePic || 'https://ui-avatars.com/api/?name=' + profile.name %>" class="w-32 h-32 rounded-full mx-auto mb-6 object-cover border-4 border-white shadow-xl bg-slate-200">
            <h1 class="text-3xl font-black"><%= profile.name %></h1>
            <p class="text-slate-400 font-bold mb-8">@<%= profile.username %></p>
            
            <div class="flex justify-center items-center gap-12 mb-8">
                <div><p class="text-2xl font-black"><%= stories.length %></p><p class="text-xs text-slate-400 font-bold uppercase">Cerita</p></div>
                <div><p class="text-2xl font-black"><%= followersCount %></p><p class="text-xs text-slate-400 font-bold uppercase">Pengikut</p></div>
            </div>

            <div class="flex justify-center gap-4">
                <% if(user && user.id === profile.id) { %>
                    <a href="/settings" class="bg-slate-900 text-white px-10 py-3 rounded-2xl font-black">Edit Profil</a>
                <% } else if(user) { %>
                    <button onclick="followUser('<%= profile.id %>')" class="bg-blue-600 text-white px-10 py-3 rounded-2xl font-black">
                        <%= isFollowing ? 'Unfollow' : 'Follow' %>
                    </button>
                <% } %>
            </div>
        </div>

        <h2 class="text-xl font-black mb-8 border-l-4 border-blue-600 pl-4">Cerita oleh <%= profile.name %></h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <% stories.forEach(s => { %>
                <a href="/story/<%= s.id %>" class="bg-white p-5 rounded-3xl flex gap-5 border border-slate-100 shadow-sm hover:shadow-md transition">
                    <img src="<%= s.coverImg %>" class="w-24 h-32 rounded-2xl object-cover bg-slate-100">
                    <div class="flex flex-col justify-center">
                        <h3 class="font-bold text-slate-800 line-clamp-2 mb-2"><%= s.title %></h3>
                        <span class="text-[10px] bg-slate-100 text-slate-500 font-black px-2 py-1 rounded-lg w-fit"><%= s.genre %></span>
                    </div>
                </a>
            <% }) %>
        </div>
    </div>
    <script>
        async function followUser(id) {
            await fetch(`/follow/${id}`, { method: 'POST' });
            location.reload();
        }
    </script>
</body>
</html>
