<!DOCTYPE html>
<html lang="id">
<head><meta charset="UTF-8"><script src="https://cdn.tailwindcss.com"></script></head>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
<body class="bg-slate-50 p-6">
    <div class="max-w-md mx-auto bg-white p-8 rounded-[2.5rem] border border-slate-200">
        <h1 class="text-2xl font-black mb-8">Pengaturan Profil</h1>
        <form action="/settings" method="POST" class="space-y-6">
            <div>
                <label class="block text-xs font-bold text-slate-400 uppercase mb-2">Ganti Nama</label>
                <input type="text" name="name" value="<%= userFull.name %>" required class="w-full p-4 bg-slate-50 border-none rounded-2xl outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            <div>
                <label class="block text-xs font-bold text-slate-400 uppercase mb-2">Foto Profil Baru</label>
                <input type="file" id="pfpInp" class="w-full text-xs text-slate-400">
                <input type="hidden" name="profilePicFile" id="pfpBase64">
            </div>
            <button class="w-full py-4 bg-blue-600 text-white rounded-2xl font-black">Simpan Perubahan</button>
            <a href="/profile/<%= userFull.id %>" class="block text-center text-slate-400 font-bold text-sm">Kembali</a>
        </form>
    </div>
    <script>
        document.getElementById('pfpInp').addEventListener('change', function() {
            const reader = new FileReader();
            reader.onload = () => document.getElementById('pfpBase64').value = reader.result;
            reader.readAsDataURL(this.files[0]);
        });
    </script>
</body>
</html>
