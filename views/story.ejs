<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= story.title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-slate-900 antialiased">
    <article class="max-w-2xl mx-auto px-6 py-10">
        <div class="flex justify-between items-center mb-10">
            <a href="/" class="text-blue-600 font-bold flex items-center gap-1">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z" clip-rule="evenodd" /></svg>
                Kembali
            </a>
            <button onclick="copyLink()" class="p-2 bg-slate-50 rounded-full hover:bg-slate-100">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z" /></svg>
            </button>
        </div>

        <img src="<%= story.coverImg %>" class="w-full h-[400px] object-cover rounded-[2.5rem] mb-10 shadow-sm border border-slate-100">

        <div class="flex items-center gap-4 mb-8">
            <img src="<%= story.authorImg %>" class="w-12 h-12 rounded-full border-2 border-white shadow-md object-cover">
            <div>
                <h4 class="font-bold text-slate-800"><%= story.authorName %></h4>
                <p class="text-xs text-slate-400 uppercase tracking-widest"><%= story.createdAt %> • <%= story.genre %></p>
            </div>
        </div>

        <h1 class="text-4xl md:text-5xl font-black text-slate-900 mb-8 leading-[1.1]"><%= story.title %></h1>

        <div class="text-slate-700 text-lg leading-relaxed whitespace-pre-line">
            <%= story.storyText %>
        </div>

        <div class="mt-16 pt-10 border-t border-slate-100 text-center">
            <p class="text-slate-400 text-sm mb-4">Sukai cerita ini? Bagikan ke teman-teman!</p>
            <button onclick="copyLink()" id="shareBtn" class="bg-blue-600 text-white px-8 py-3 rounded-full font-bold hover:scale-105 transition">
                Salin Link Cerita
            </button>
        </div>
    </article>

    <script>
        function copyLink() {
            navigator.clipboard.writeText(window.location.href);
            const btn = document.getElementById('shareBtn');
            btn.innerText = 'Link Disalin!';
            btn.classList.replace('bg-blue-600', 'bg-green-500');
            setTimeout(() => {
                btn.innerText = 'Salin Link Cerita';
                btn.classList.replace('bg-green-500', 'bg-blue-600');
            }, 2000);
        }
    </script>
</body>
</html>
