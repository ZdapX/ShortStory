<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8"><script src="https://cdn.tailwindcss.com"></script></head>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
<body class="bg-slate-50 p-6">
    <div class="max-w-2xl mx-auto bg-white p-8 rounded-[2.5rem] border border-slate-200">
        <h1 class="text-2xl font-black mb-8">Tulis Cerita Baru</h1>
        <form action="/upload" method="POST" class="space-y-6">
            <div>
                <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest mb-2">Judul Cerita</label>
                <input type="text" name="title" required class="w-full p-4 bg-slate-50 border border-slate-100 rounded-2xl outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest mb-2">Pilih Genre</label>
                    <select name="genre" class="w-full p-4 bg-slate-50 border border-slate-100 rounded-2xl outline-none">
                        <option>Romance</option><option>Horror</option><option>Action</option><option>Fantasy</option><option>Slice of Life</option>
                    </select>
                </div>
                <div>
                    <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest mb-2">Cover (File Gambar)</label>
                    <input type="file" id="fileInp" class="w-full text-xs text-slate-500 file:bg-blue-50 file:text-blue-700 file:border-0 file:py-2 file:px-4 file:rounded-full file:mr-4">
                    <input type="hidden" name="coverImgFile" id="base64Inp">
                </div>
            </div>
            <div>
                <label class="block text-xs font-bold text-slate-400 uppercase tracking-widest mb-2">Teks Cerita</label>
                <textarea name="storyText" rows="10" required class="w-full p-4 bg-slate-50 border border-slate-100 rounded-2xl outline-none focus:ring-2 focus:ring-blue-500"></textarea>
            </div>
            <button class="w-full py-4 bg-blue-600 text-white rounded-2xl font-black shadow-lg shadow-blue-100">Publikasikan Cerita</button>
            <a href="/" class="block text-center text-slate-400 font-bold text-sm">Batal & Kembali</a>
        </form>
    </div>
    <script>
        document.getElementById('fileInp').addEventListener('change', function() {
            const reader = new FileReader();
            reader.onload = () => document.getElementById('base64Inp').value = reader.result;
            reader.readAsDataURL(this.files[0]);
        });
    </script>
</body>
</html>
